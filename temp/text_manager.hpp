#pragma once
#include "SDL.h"
//#include "SDL_TTF.h"
#include "game.hpp"

class TextManager{
public:
    // static TTF_Font* LoadFont(const char* font_filename){
    //     std::cout << "ohio" << std::endl;
    //     TTF_Font* font = TTF_OpenFont(font_filename, 20);
    //     if (!font) {
    //         std::cerr << "Failed to load font: " << TTF_GetError() << std::endl;
    //     }
    //     return font;
    // }
};